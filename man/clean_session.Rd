% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clean_session.R
\name{clean_session}
\alias{clean_session}
\title{Clean up an R session conservatively}
\usage{
clean_session(
  remove_objects = TRUE,
  run_gc = TRUE,
  unload_packages = FALSE,
  remove_tempfiles = TRUE,
  verbose = TRUE
)
}
\arguments{
\item{remove_objects}{Logical. Remove objects from the global environment. Default TRUE.}

\item{run_gc}{Logical. Run \code{\link[base:gc]{base::gc()}} at the end and return its summary invisibly. Default TRUE.}

\item{unload_packages}{Logical. Unload non-base namespaces. Default FALSE.}

\item{remove_tempfiles}{Logical. Delete files under \code{\link[base:tempfile]{base::tempdir()}}. Default TRUE.}

\item{verbose}{Logical. Emit progress messages. Default TRUE.}
}
\value{
Invisibly returns the result of \code{\link[base:gc]{base::gc()}} if run, otherwise \code{NULL}.
}
\description{
Performs common end-of-script cleanup tasks: closes graphics devices,
stops parallel clusters referenced in the global environment, closes open
connections, disconnects lingering DBI connections, deletes files in the
session temporary directory, optionally unloads non-base namespaces, removes
objects from the global environment, and triggers garbage collection.
}
\examples{
\dontrun{
  # Register for automatic cleanup on exit
  on.exit(clean_session(), add = TRUE)
  # ... your analysis ...
  clean_session()
}
}
